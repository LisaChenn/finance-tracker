# Sentry DSN for the client. Only VITE_-prefixed vars are exposed to
# the browser bundle. Leave blank to disable error reporting.
# Get one at https://sentry.io by creating a React project.
VITE_SENTRY_DSN=
